import { createStore } from 'vuex';
import pokemon from './pokemon';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    pokemon,
  },
});
